import $forms from './data/_.flat';
import computed from './computed';

computed.isValid($forms);
computed.hasError($forms);
computed.isDirty($forms);
computed.isEmpty($forms);
computed.isPristine($forms);
computed.isPristine($forms);
